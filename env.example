# Upstash Redis, added through the Vercel Marketplace.
# The integration injects one of these two naming schemes. Either one works;
# the counter route reads whichever exists and disables itself if neither does.

KV_REST_API_URL=
KV_REST_API_TOKEN=

# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=

# Optional. Defaults to https://upitax.vercel.app
# NEXT_PUBLIC_SITE_URL=
